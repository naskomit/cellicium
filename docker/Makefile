cellicium/build:
	docker-compose build cellicium

cellicium/up:
	docker-compose up cellicium

cellicium/prebuilt:
	docker-compose -f docker-compose.yaml -f docker-compose.prebuilt.yaml up --build cellicium

cellicium/bash:
	docker-compose exec cellicium /bin/bash
