from setuptools import find_packages, setup

setup(
    name = 'cellicium',
    packages = find_packages(include = ['cellicium']),
    version = '0.1.0',
    description='Single-cell library',
    author='Atanas Pavlov / SysMo',
    license='MIT',
)
